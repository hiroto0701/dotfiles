{
  "mcpServers": {
    "backlog": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--pull",
        "always",
        "-e",
        "BACKLOG_DOMAIN",
        "-e",
        "BACKLOG_API_KEY",
        "ghcr.io/nulab/backlog-mcp-server"
      ],
      "env": {
        "BACKLOG_DOMAIN": "bnx.backlog.com",
        "BACKLOG_API_KEY": {{ if hasKey . "backlogApiKey" }}"{{ index . "backlogApiKey" }}"{{ else }}""{{ end }}
      }
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "tavily": {
      "url": {{ if hasKey . "tavilyApiKey" }}"https://mcp.tavily.com/mcp/?tavilyApiKey={{ .tavilyApiKey }}"{{ else }}""{{ end }}
    }
  }
}
